# Raspberry Pi Pico 2 PIO demos

The pio folder includes some examples. Copy the code into test.pio for a quick demo.

They will execute on PIO block 0 on state machine 0. The output will be on GPIO pin 4.

The main code also includes an LED heart beat.